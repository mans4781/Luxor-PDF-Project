{
  "name": "@workspace/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "hello": "tsx ./src/hello.ts",
    "keys:generate": "tsx ./src/generate-keys.ts",
    "invoices:pdf": "tsx ./src/generate-invoice-pdf.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@workspace/db": "workspace:*",
    "@workspace/license-keys": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "pdf-lib": "^1.17.1",
    "playwright": "^1.60.0",
    "tsx": "catalog:"
  }
}
