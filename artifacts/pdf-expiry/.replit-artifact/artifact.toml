kind = "web"
previewPath = "/pdf-expiry/"
title = "PDF Security Tool"
version = "1.0.0"
id = "artifacts/pdf-expiry"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/pdf-expiry/" ]
localPort = 19050

[services.development]
run = "pnpm --filter @workspace/pdf-expiry run dev"

[services.env]
PORT = "19050"
BASE_PATH = "/pdf-expiry/"
